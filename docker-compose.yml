services:
  live-paper:
    build: .
    container_name: trading-bot-live
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./live_paper_logs:/app/live_paper_logs
    command: ["--instruments", "NQ", "ES", "RTY", "YM", "--signal-only", "YM"]
